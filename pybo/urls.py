from django.contrib import admin
from django.urls import include, path
# from .views import index_view, issue_view


app_name = 'pybo'

urlpatterns = [

    # path('', index_view.index, name='index'),
    # path('issue/', issue_view.issue, name='issue'),

]